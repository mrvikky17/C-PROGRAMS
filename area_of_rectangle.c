#include <stdio.h>
int main()
{
      int l,b,area;
      printf("enter length\n enter breadth\n");
      scanf("%d%d",&l,&b);;
      area=l*b;
      printf("area=%d", area );
      return 0;
}