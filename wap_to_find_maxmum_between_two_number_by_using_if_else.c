#include <stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if (a>b)
    {
        printf("largest no is=%d ",a);
    }
    else 
    {
        printf("largest no is=%d",b);
    }
    return 0;
}